const User = {
    name: "Priyank",
    email: "priyank@gmail.com",
    isActive: true
}

function createUser({name, isPaid}: {name: string, isPaid: boolean}) {}

createUser({name: "Priyank", isPaid: false})

function createCourse(): {} {
    return {}
}